services:
  portfolio:
    container_name: portfolio
    image: musenkishi/portfolio:latest
    # user: ${PUID}:${PGID} # optional
    # volumes:
      # - data:/data
    # env_file:
      # - .env
    # environment:
    #   - GITHUB_ACCESS_TOKEN="ghp_ABC123"
    env_file:
      - .env.local
    ports:
      - 3000:3000
    restart: unless-stopped

# volumes:
  # data:
